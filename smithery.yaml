startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "node",
      "args": [
        "dist/index.js"
      ],
      "env": {}
    })
build:
  dockerfile: Dockerfile-mcp
